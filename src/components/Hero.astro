---
import DottedSurface from './ui/DottedSurface.astro';

export interface Props {
  lang?: 'en' | 'de';
}
const { lang = 'en' } = Astro.props;
---

<section class="hero" id="hero">
  <DottedSurface />

  <div class="hero__inner container">
    <div class="hero__badge">
      <span class="hero__badge-dot" aria-hidden="true"></span>
      {lang === 'de' ? 'KI-gestützte Belegschaftsintelligenz' : 'AI-powered workforce intelligence'}
    </div>

    <h1 class="hero__headline">
      <span class="hero__line1">
        {lang === 'de' ? 'Die Antworten sind' : 'The answers are'}
      </span>
      <span class="hero__line2">
        {lang === 'de' ? 'bereits in Ihrem Unternehmen.' : 'already within your organization.'}
      </span>
    </h1>

    <p class="hero__sub">
      {lang === 'de'
        ? 'inplicit.ai gibt jedem in Ihrer Organisation eine Stimme. Verstehen Sie, was Ihre Belegschaft wirklich weiß, denkt und bewegt.'
        : 'inplicit.ai gives every person in your organisation a voice. Understand what your workforce truly knows, thinks, and needs.'}
    </p>

    <div class="hero__actions">
      <a href="#request-pilot" class="hero__cta-primary">
        <span class="hero__cta-primary-label">
          {lang === 'de' ? 'Zur Warteliste' : 'Join Waitlist'}
        </span>
        <i class="hero__cta-primary-icon" aria-hidden="true">
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
            <path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </i>
      </a>
      <a href="#how-it-works" class="hero__cta-secondary">
        {lang === 'de' ? 'Wie es funktioniert' : 'How it works'}
      </a>
    </div>

    <div class="hero__meta">
      <span class="hero__meta-item hero__meta-item--check">
        {lang === 'de' ? 'DSGVO-konform' : 'GDPR compliant'}
      </span>
      <span class="hero__meta-sep" aria-hidden="true">·</span>
      <span class="hero__meta-item hero__meta-item--check">
        {lang === 'de' ? 'Betriebsrat-ready' : 'Works-council ready'}
      </span>
      <span class="hero__meta-sep" aria-hidden="true">·</span>
      <span class="hero__meta-item hero__meta-item--check">
        {lang === 'de' ? 'Daten in Frankfurt (EU)' : 'Data in EU'}
      </span>
      <span class="hero__meta-sep" aria-hidden="true">·</span>
      <span class="hero__meta-item hero__meta-item--check">
        {lang === 'de' ? 'EU AI Act konform' : 'EU AI Act compliant'}
      </span>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100svh;
    display: flex;
    align-items: center;
    padding: var(--space-section-lg) 0 var(--space-section);
  }

  @media (max-width: 767px) {
    .hero {
      padding: var(--space-32) 0 var(--space-24);
      min-height: 90svh;
    }
  }

  .hero__inner {
    position: relative;
    z-index: 1;
    max-width: 900px;
    animation: hero-in 0.7s var(--ease-out) forwards;
  }

  @keyframes hero-in {
    from { opacity: 0; transform: translateY(20px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  /* Badge */
  .hero__badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    border: 1px solid var(--color-border);
    color: var(--color-text-secondary);
    font-size: var(--text-xs);
    font-weight: 600;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    padding: var(--space-1) var(--space-3);
    border-radius: var(--radius-full);
    margin-bottom: var(--space-8);
    animation: hero-in 0.7s var(--ease-out) forwards 0.05s;
    opacity: 0;
  }

  .hero__badge-dot {
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background-color: var(--color-accent);
    animation: pulse 2.5s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50%       { opacity: 0.5; transform: scale(1.4); }
  }

  /* Headline */
  .hero__headline {
    font-size: clamp(var(--text-2xl), 8vw, var(--text-4xl));
    font-weight: 800;
    line-height: 1.0;
    letter-spacing: -0.04em;
    margin-bottom: var(--space-8);
    animation: hero-in 0.7s var(--ease-out) forwards 0.1s;
    opacity: 0;
    display: flex;
    flex-direction: column;
    gap: 0.04em;
  }

  .hero__line1 { color: var(--color-text-primary); }
  .hero__line2 { color: var(--color-text-primary); opacity: 0.32; }

  .hero__sub {
    font-size: clamp(var(--text-base), 2vw, var(--text-lg));
    color: var(--color-text-secondary);
    line-height: 1.75;
    max-width: 560px;
    margin-bottom: var(--space-10);
    animation: hero-in 0.7s var(--ease-out) forwards 0.18s;
    opacity: 0;
  }

  .hero__actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--space-3);
    margin-bottom: var(--space-10);
    animation: hero-in 0.7s var(--ease-out) forwards 0.26s;
    opacity: 0;
  }

  /* Primary CTA — monochromatic inversion */
  .hero__cta-primary {
    position: relative;
    overflow: hidden;
    display: inline-flex;
    align-items: center;
    height: 48px;
    padding: 0 var(--space-8);
    background-color: var(--color-cta-bg);
    color: var(--color-cta-text);
    font-size: var(--text-base);
    font-weight: 700;
    letter-spacing: -0.01em;
    border-radius: var(--radius-ui);
    transition: opacity 0.2s var(--ease-smooth);
  }

  .hero__cta-primary:hover { opacity: 0.85; }
  .hero__cta-primary:active { transform: scale(0.97); }

  .hero__cta-primary-label {
    margin-right: var(--space-10);
    transition: opacity 0.4s var(--ease-smooth);
    white-space: nowrap;
  }

  .hero__cta-primary:hover .hero__cta-primary-label { opacity: 0; }

  .hero__cta-primary-icon {
    position: absolute;
    right: 4px;
    top: 4px;
    bottom: 4px;
    border-radius: var(--radius-ui);
    z-index: 1;
    display: grid;
    width: 25%;
    place-items: center;
    background-color: var(--color-cta-icon-bg);
    transition: width 0.4s var(--ease-spring);
  }

  .hero__cta-primary:hover .hero__cta-primary-icon { width: calc(100% - 8px); }

  /* Secondary CTA */
  .hero__cta-secondary {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-3) var(--space-6);
    color: var(--color-text-secondary);
    font-size: var(--text-base);
    font-weight: 500;
    border-radius: var(--radius-ui);
    border: 1px solid var(--color-border);
    transition: color 0.15s var(--ease-smooth), border-color 0.15s var(--ease-smooth);
    height: 48px;
  }

  .hero__cta-secondary:hover {
    color: var(--color-text-primary);
    border-color: var(--color-text-tertiary);
  }

  /* Meta */
  .hero__meta {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--space-3);
    animation: hero-in 0.7s var(--ease-out) forwards 0.34s;
    opacity: 0;
  }

  .hero__meta-item {
    font-size: var(--text-sm);
    color: var(--color-text-tertiary);
    font-weight: 400;
  }

  .hero__meta-item--check::before {
    content: '✓ ';
    color: var(--color-accent);
    font-weight: 600;
  }

  .hero__meta-sep {
    color: var(--color-text-tertiary);
    font-size: var(--text-sm);
    opacity: 0.5;
  }

  @media (max-width: 767px) {
    .hero__sub { font-size: var(--text-base); }
    .hero__meta-sep { display: none; }
    .hero__meta { flex-direction: column; align-items: flex-start; gap: var(--space-2); }
  }

  @media (max-width: 479px) {
    .hero__actions { flex-direction: column; align-items: stretch; }
    .hero__cta-primary, .hero__cta-secondary { width: 100%; justify-content: center; }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero__inner, .hero__badge, .hero__headline,
    .hero__sub, .hero__actions, .hero__meta {
      animation: none;
      opacity: 1;
    }
    .hero__badge-dot { animation: none; }
  }
</style>
